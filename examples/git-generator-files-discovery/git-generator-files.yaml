apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: guestbook
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/salazarch/applicationset.git
        revision: HEAD
        files:
          - path: "examples/git-generator-files-discovery/cluster-config/**/config.json"
    - list:
        elements:
          - name: guestbook
            image: "0.1"  # Ensure this is a string
  template:
    metadata:
      name: '{{name}}-guestbook'
    spec:
      project: default
      source:
        repoURL: https://github.com/salazarch/applicationset.git
        targetRevision: HEAD
        path: "examples/git-generator-files-discovery/apps/guestbook"
        kustomize:
          images:
            - 'gcr.io/heptio-images/ks-guestbook-demo:5'
      destination:
        server: '{{cluster.address}}'
        namespace: guestbook
